# n8n Workflow

The exported n8n workflow will be added in Phase 8.

The workflow will receive successful focus-session events from the backend, dedupe by stable event ID, send a mock notification, and confirm delivery back to the backend.
