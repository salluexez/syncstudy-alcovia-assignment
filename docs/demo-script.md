# Demo Script

This file will be completed after the implementation phases.

The final demo must show:

- Two isolated clients on the same student account.
- Both clients going offline.
- Offline focus session completion on each client.
- A conflicting task edit across devices.
- Reconnect and sync.
- Both devices converging to the same state.
- n8n notification firing exactly once per successful focus session.
